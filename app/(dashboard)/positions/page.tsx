/**
 * Positions Page
 * 
 * Responsibility: List all individual holdings in the portfolio.
 * Owner: Frontend Engineer
 * Implementation: Fetch positions using usePortfolio hook and render them in a sortable table.
 */

import React from 'react';
// import { usePortfolio } from '@/hooks/usePortfolio';

export default function PositionsPage() {
  return (
    <div className="flex flex-col items-center justify-center min-h-screen">
      <h1 className="text-2xl font-bold">Positions</h1>
      <p className="text-gray-500 mt-2">NOT BUILT YET — Awaiting the developer to implement the positions table/list.</p>
    </div>
  );
}

